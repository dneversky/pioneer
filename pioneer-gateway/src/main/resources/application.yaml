server:
  port: 8080

spring:
  main:
    web-application-type: reactive
  application:
    name: api-gateway

grpc:
  server:
    port: 0
  client:
    GLOBAL:
      negotiationType: plaintext

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka