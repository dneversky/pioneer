server:
  port: 0

spring:
  application:
    name: api-gateway

grpc:
  server:
    port: 0
  client:
    GLOBAL:
      negotiationType: plaintext

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka