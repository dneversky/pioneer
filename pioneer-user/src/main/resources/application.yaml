server:
  port: 0

spring:
  application:
    name: user-service
  data:
    mongodb:
      authentication-database: admin
      username: root
      password: root
      database: user
      port: 27017
      host: localhost
      auto-index-creation: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka